%h1 Companies with bonds tracked by BondPros
%h4 View, Edit or Delete the Companies in the database
%table{:class => "table table-condensed table-striped"}
	%tr
	%th{:align => "left"} ID
	%th{:align => "left"} Company
	%th Ticker
	%th Website
	%th Exchange
	%tbody
		- @companies.each do |company|
			%tr			
				%td
					= company.id
				%td
					= company.company_name
				%td
					= company.ticker
				%td{:align => "center"}
					= link_to company.url, "http://www." + company.url, :target => "_blank"
				%td{:align => "center"}
					= company.exchange.exchange_name
				- if can? :update, @companies
					%td
						= link_to 'Edit', '/admin/company/' + company.id.to_s
				- if can? :destroy, @companies
					%td
						= link_to 'Delete', '/admin/company/delete/' + company.id.to_s
